<?xml version="1.0" encoding="utf-8"?>
<annotations version="1.0">

	<metadata name="Context" description="Injects the IApplicationContext in charge of the object">
		<context name="method" />
		<context name="getter" />
		<context name="setter" />
		<context name="variable" />
	</metadata>

	<metadata name="ProcessOnEvent" description="Start processing this object once it dispatches the given event">
		<context name="class">
			<attribute name="event" type="String" required="true" />
		</context>
	</metadata>

</annotations>
