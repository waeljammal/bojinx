package com.bojinx.api.context.config
{
	/**
	 * Defines a contract for a container that
	 * can store Beans and other Configuration elements.
	 * 
	 * @see com.bojinx.api.context.config.IBean
	 * 
	 * @author Wael Jammal
	 */
	public interface IBeans extends IConfigItem
	{
	}
}
