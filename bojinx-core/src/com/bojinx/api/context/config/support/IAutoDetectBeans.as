package com.bojinx.api.context.config.support
{
	
	/**
	 * Attempts to detect beans based on the member type that is
	 * being annotated and passes that information accross to the
	 * processor, this saves you having to write the look up code.
	 *
	 * @author Wael Jammal
	 */
	public interface IAutoDetectBeans
	{
	}
}
